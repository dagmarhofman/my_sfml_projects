/* om herdefinities te voorkomen, heb ik de variabelen voor filenames in eigen modules gezet */

enum direction { up, down, left, right };

const int icon_size_x = 60;
const int icon_size_y = 65;

// add more sprites in later version.
const int max_sprites = 6;
const int start_level = 1;

// milliseconds for one player step
const float sprite_velocity = 100;
const float move_velocity = 2.5;
const float push_velocity = 300;
const int left_key = 71;
const int right_key = 72;
const int up_key = 73;
const int down_key = 74;
const int window_x_size = 795;
const int window_y_size = 585;
const int amount_of_levels = 3;

